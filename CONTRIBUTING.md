# Contributing

Use branch-first, PR-first flow for all changes.

## Rules

- Use conventional commits.
- Keep scripts idempotent when possible.
- Prefer explicit, readable shell over dense one-liners.
- Include usage examples for every new command.

## Local check

```bash
bash -n scripts/*.sh
```
